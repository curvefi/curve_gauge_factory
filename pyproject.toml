[project]
name = "curve-gauge-factory"
version = "6.1.1"
description = "Gauge Factory for CRV emissions and rewards"
readme = "README.md"
requires-python = ">=3.10.4"
dependencies = [
    "vyper==0.4.3",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "titanoboa>=0.2.8",
]
